/*
安裝這個模組，之後要生成component只要打 `rcc` 就可以自動生成如以下 template
Name: ES7+ React/Redux/React-Native snippets
Publisher: dsznajder
*/

import React, { Component } from 'react'

export default class Test extends Component {
  render() {
    return (
      <div>Test</div>
    )
  }
}
